name: Example Workflow

on:
  push:
    branches:
      - main

env:
  VARIABLE_NAME: "value"

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Job 1
        run: echo "Running Job 1"

      - name: Use Environment Variable
        run: echo "Environment Variable: $VARIABLE_NAME"

      # Additional steps for Job 1

  job2:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Job 2
        run: echo "Running Job 2"

      - name: Use Environment Variable
        run: echo "Environment Variable: $VARIABLE_NAME"

      # Additional steps for Job 2

  # Other jobs in the workflow
